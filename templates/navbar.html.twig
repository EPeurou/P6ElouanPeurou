<nav class="navbar navbar-expand-lg fixed-top" id="mainNav">
    <div class="container px-4 px-lg-5">
        <h3 class="text-uppercase title-nav remove-title-nav">SnowTricks</h3>
        <ul class="navbar-nav">
            <div class="row row-nav mb-3" style="margin-left:6em;">
                <div class="col-sm-4 w-auto col-home">
                    <li class="nav-item"><a class="nav-link link-home" href="{{ path('main')}}">Accueil</a></li>
                </div>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <div class="col-sm-4 w-auto">
                    <li class="nav-item"><a class="nav-link link-add" href="{{ path('tricks_new') }}">Ajouter un trick</a></li>
                </div>
                <div class="col-sm-4 w-auto">
                    <li class="nav-item"><a class="nav-link link-logout" href="{{ path('app_logout') }}">Déconnexion</a></li>
                </div>
                {% else %}
                <div class="col-sm-4 w-auto">
                    <li class="nav-item"><a class="nav-link link-login" href="{{ path('app_login') }}">Connexion</a></li>
                </div>
                <div class="col-sm-4 w-auto">
                    <li class="nav-item"><a class="nav-link link-register" href="{{ path('user_new') }}">Inscription</a></li>
                </div>
              {% endif %}
            </div>
        </ul>
        
    </div>
</nav>